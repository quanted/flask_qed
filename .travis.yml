language: python

#version
python:
  - "2.7"

# command to install dependencies
install: "pip install -r requirements.txt"

# command to run tests
script: nosetests --with-coverage --cover-erase --cover-package=ubertool_ecorest.REST_UBER.sip_rest.sip_model_rest -v
script: nosetests --with-coverage --cover-erase --cover-package=ubertool_ecorest.REST_UBER.stir_rest.stir_model_rest -v
